#include "bitstream.h"

/*
 * all bitstream logic is inline in header for performance 
 * this TU exists to satisfy the build system 
 * will hold any future non inline bitstream utilities
*/

typedef int bitstream_dummy;
